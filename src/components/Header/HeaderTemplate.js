import styles from './Header.module.sass';

let HeaderTemplate = () => {
    return (
        <header className={styles.wrapper}>
            <div>ReactApp</div>
        </header>
    )
}
export default HeaderTemplate;